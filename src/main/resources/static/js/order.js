window.onload = function () {
    initNavigation();
    initShoppingCart();
};

function initShoppingCart() {
    let hero = $('hero');
    hero.style.backgroundImage = 'url("../img/tongji/bg/' + '08' + '.png")';
}